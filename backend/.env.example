# Servidor
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=beristain_db
DB_USER=beristain_user
DB_PASSWORD=tu_password_seguro_aqui

# JWT
JWT_SECRET=tu_secret_jwt_muy_seguro_y_largo_aqui_cambiar_en_produccion
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=https://tudominio.com,http://localhost:8080

# Backups
BACKUP_DIR=/var/backups/beristain
BACKUP_SCHEDULE=0 2 * * *

# Logs
LOG_LEVEL=info
LOG_DIR=/var/log/beristain

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
