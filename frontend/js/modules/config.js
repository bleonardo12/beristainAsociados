// /modules/config.js
export const DEFAULT_CONFIG = {
    theme: 'light',
    animationSpeed: 300,
    // Añade aquí cualquier otra constante que tus módulos usen
};

export const createNavbarAPI = () => {
    return {
        // Lógica de tu API de navegación
    };
};